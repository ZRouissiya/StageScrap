"use strict";

var _schema = require("../../schema");

_schema.Schema.Type.ref = {
  validate: {
    required: (required, data) => !required || !!data ? [] : [{
      message: 'is required'
    }],
    validate: (validate, data) => validate(data)
  }
};
//# sourceMappingURL=index.js.map